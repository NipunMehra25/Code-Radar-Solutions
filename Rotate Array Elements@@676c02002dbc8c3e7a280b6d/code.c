#include<stdio.h>
int main(){
    int n,k;
    int arr[n];


    scanf("%d",&n);

    for(int i = 0 ; i < n; i++){
        scanf("%d",arr[i]);
    }

    scanf("%d",&k);

    k = k % n;
    for(int i = 0 ; i < k ; i++){

        }
    }
}