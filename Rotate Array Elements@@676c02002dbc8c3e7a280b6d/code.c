#include<stdio.h>
int main(){
    int n,k;
    int arr[n];


    scanf("%d",&n);

    for(int i = 0 ; i < n; i++){
        scanf("%d",arr[i]);
    }

    scanf("%d",&k);

    for(int i = 0 ; i < k ; i++){
        for(int j = 0 ; j < n ; j++){
            int temp1 = arr[0];
            arr[]
        }
    }
}